const baseStyle = {
  fontFamily: 'body',
  fontWeight: 400,
  fontStyle: 'normal',
  fontSize: '0.875rem',
  lineHeight: '144%',
  color: 'primary.base',
};

const config = {
  baseStyle,
};

export default config;
